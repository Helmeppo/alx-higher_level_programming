#!/usr/bin/python3
def square_matrix_s
    def square_cells(array):
        result = []
        for cell in array:
            result.append(cell ** 2)
        return resultimple(matrix=[]):
    return [list(map(lambda x: x ** 2, row)) for row in matrix]
